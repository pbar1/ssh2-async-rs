[package]
name = "async-ssh2"
version = "0.1.0"
edition = "2024"

[features]
default = ["tokio"]
tokio = ["dep:tokio"]

[dependencies]
futures = "0.3"
ssh2 = "0.9.5"
tokio = { version = "1", optional = true, features = ["macros", "net", "rt"] }
